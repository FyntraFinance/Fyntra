export const metadata = {
  title: "Dashboard — Fyntra",
};

/**
 * O conteúdo real é renderizado pelo Shell (aba ativa, sem navegação) — os
 * dados de todas as abas já vêm prontos do layout. Esta rota existe só para
 * o Next ter um destino: sem ela `/dashboard` não seria uma página válida.
 */
export default function DashboardPage() {
  return null;
}
